# Web Application & API (Railway deployment)
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# Core RAG pipeline (no external model downloads required)
rank_bm25==0.2.2

# LLM generation (Ollama) -- requires Ollama running locally (ollama serve)
ollama>=0.4.0

# Testing
pytest>=8.0.0

# Tiny "is this a real error" classifier (src/classifier/) -- TF-IDF + Logistic
# Regression, no model download needed
scikit-learn>=1.4.0
joblib>=1.3.0

# Optional: only needed if you run src/data_generation/scrape_oracle_docs.py
# to expand the knowledge base from Oracle's official docs
requests>=2.31.0
beautifulsoup4>=4.12.0

# Optional upgrade path (not used yet -- see retriever.py docstring for why
# BM25 was chosen for the MVP, and what swapping to dense embeddings would
# look like):
# sentence-transformers>=2.7.0
# chromadb>=0.5.0

# Local T5/BART inference (src/rag/generator.py's generate_t5()) -- only
# needed if you're using mode="t5"/"auto" with a trained model present at
# models/oracle_log_t5_model. Heavy installs, commented out by default.
# torch>=2.0.0
# transformers>=4.46.0
# sentencepiece>=0.2.0
